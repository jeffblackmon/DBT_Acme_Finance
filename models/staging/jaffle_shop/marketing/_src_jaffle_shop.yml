sources:
  - name: jaffle_shop
    database: raw
    schema: jaffle_shop
    config:
      freshness: 
        warn_after:
          count: 12
          period: hour 
        error_after:
          period: hour
          count: 24
      loaded_at_field: _etl_loaded_at   
    tables:
      - name: customers
        freshness: null    
      - name: orders
    